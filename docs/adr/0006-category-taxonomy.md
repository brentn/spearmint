# Small hierarchical category taxonomy replaces Plaid's flat list

Categories are a small, two-level hierarchical set (parent/child) modeled after mint.com's mobile app, replacing the old Plaid-derived flat taxonomy of roughly 100 entries. `CategoryType` is `'expense' | 'income' | 'transfer'` — Income is a first-class category type, not a sign convention on expense categories.
