# Backup & sync: export/import only, single-device by design

Export/import (with an optional encryption toggle) is the only backup mechanism and the only way data moves between devices — Spearmint is single-device by design, with no real-time or background multi-device sync.
